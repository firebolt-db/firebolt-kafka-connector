<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    
    <!-- Properties for configuration -->
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_DIR">logs</Property>
        <Property name="LOG_FILE">firebolt-kafka-connector</Property>
    </Properties>
    
    <!-- Appenders -->
    <Appenders>
        
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
        
        <!-- Rolling File Appender -->
        <RollingFile name="RollingFile" 
                     fileName="${LOG_DIR}/${LOG_FILE}.log"
                     filePattern="${LOG_DIR}/${LOG_FILE}-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <!-- Roll over daily -->
                <TimeBasedTriggeringPolicy />
                <!-- Roll over when file reaches 10MB -->
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30">
                <!-- Delete files older than 30 days -->
                <Delete basePath="${LOG_DIR}" maxDepth="1">
                    <IfFileName glob="${LOG_FILE}-*.log.gz" />
                    <IfLastModified age="30d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
        
        <!-- Async Appender for better performance -->
        <Async name="AsyncRollingFile">
            <AppenderRef ref="RollingFile"/>
        </Async>
        
    </Appenders>
    
    <!-- Loggers -->
    <Loggers>
        
        <!-- Firebolt Kafka Connector Loggers -->
        <Logger name="com.firebolt" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        
        <!-- Kafka Connect Loggers -->
        <Logger name="org.apache.kafka.connect" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        
        <!-- Kafka Loggers - reduce noise -->
        <Logger name="org.apache.kafka" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        
        <!-- Apache HTTP Client - reduce noise -->
        <Logger name="org.apache.http" level="WARN" />
        
        <!-- Schema Registry Client - reduce noise -->
        <Logger name="io.confluent" level="WARN" />
        
        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncRollingFile"/>
        </Root>
        
    </Loggers>
    
</Configuration> 